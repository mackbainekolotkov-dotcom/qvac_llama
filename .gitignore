# Build trees
/build/
/build-*/
*.o
*.a
*.dylib
*.so

# Downloaded models and checkpoints. 20 GB, and single files far above GitHub's
# 100 MB limit. Reproduced by scripts/qvac/00..02 and 07, byte for byte.
/models/qvac/
/models/ggml-vocabs/

# HuggingFace bookkeeping
.cache/

# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# NOTE: logs/ and patches/ are deliverables and are deliberately NOT ignored.
# Upstream llama.cpp ignores *.log; that rule is intentionally absent here.
