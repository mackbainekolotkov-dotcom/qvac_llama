# Appendix - subjective quality, side by side

Same seed (42), same token budget, `-ngl 99`. Generated by `scripts/qvac/05_subjective.sh`;
raw logs in `logs/task1/subjective/`. Prompts 1 and 2 are the ones from the brief, 3 to 5 are fixed extras.


## Prompt 1

> What is bitcoin?


### F16

```
Bitcoin is a decentralized digital currency that uses cryptography for secure financial transactions. It was created in 2009 by an individual or group of individuals using the pseudonym Satoshi Nakamoto. Bitcoin operates on a peer-to-peer network, meaning that transactions are recorded on a public ledger called the blockchain, without the need for a central authority or intermediary.

Here are some key features of Bitcoin:

1. **Decentralized**: Bitcoin is not controlled by any government or institution. Transactions are recorded on a public ledger
```


### Q4_0

```
Bitcoin is a digital or virtual currency that uses cryptography for security and is decentralized, meaning it's not controlled by any government or financial institution. It was created in 2009 by an individual or group of individuals using the pseudonym Satoshi Nakamoto.

Bitcoin is based on a peer-to-peer network that allows users to send and receive funds without the need for intermediaries like banks. Transactions are recorded on a public ledger called the blockchain, which helps to prevent counterfeiting and ensures the integrity of the
```


### Q4_1

```
Bitcoin is a decentralized digital currency that uses cryptography for security and is decentralized, meaning it's not controlled by any government or financial institution. Here's a brief overview:

**Key characteristics:**

1. **Decentralized**: Bitcoin is not issued or controlled by a central authority, such as a government or bank. Transactions are recorded on a public ledger called the blockchain.
2. **Digital**: Bitcoin exists only in digital form, and transactions are made through the internet.
3. **Limited supply**:
```


### Q4_HQQ

```
Bitcoin is a digital currency that uses cryptography for security and is decentralized, meaning it's not controlled by any government or financial institution. It was created in 2009 by an individual or group of individuals using the pseudonym Satoshi Nakamoto.

Here's a brief overview of how bitcoin works:

1. **Decentralized network**: Bitcoin is stored on a public ledger called the blockchain, which is maintained by a network of computers around the world. This decentralized network allows for secure and transparent transactions.
2
```


### Q4_HQQ + optimizer

```
Bitcoin is a digital currency that uses cryptography to secure and verify transactions. It was created in 2009 by an individual or group of individuals using the pseudonym Satoshi Nakamoto. Bitcoin is decentralized, meaning it's not controlled by any government or financial institution, and is instead maintained by a network of computers around the world.

Here's a brief overview of how bitcoin works:

1. **Transactions**: When a user wants to send bitcoin to another user, they create a transaction and broadcast it to the
```


## Prompt 2

> Write a Python function that reverses a list.


### F16

```
## Reversing a List in Python

You can use the built-in `reverse()` method or slicing to reverse a list in Python.

### Using the `reverse()` Method

```python
def reverse_list_reverse_method(input_list):
    """
    Reverses a list using the reverse() method.

    Args:
        input_list (list): The list to be reversed.

    Returns:
        list: The reversed list.
    """
    input_list.reverse()
    return input_list

# Example usage:
my_list = [1, 2, 3, 4, 5]
print("Original List:", my_list)
print("Reversed List:", reverse_list_reverse_method(my_list))
```

### Using Slicing

```python
def reverse_list_slicing(input_list):
    """
    Reverses a list using slicing.

    Args:
        input_list (list): The list to be reversed.

    Returns:
        list: The reversed list.
    """
    return input_list[::-1]

# Example usage:
my_list = [1, 2, 3,
```


### Q4_0

```
**Reversing a List in Python**
================================

Here is a Python function that uses slicing to reverse a list:

```python
def reverse_list(input_list):
    """
    Reverses a list using slicing.

    Args:
        input_list (list): The list to be reversed.

    Returns:
        list: The reversed list.
    """
    return input_list[::-1]

# Example usage:
my_list = [1, 2, 3, 4, 5]
reversed_list = reverse_list(my_list)
print(reversed_list)  # Output: [5, 4, 3, 2, 1]
```

This function works by using Python's slice notation, `input_list[start:stop:step]`. In this case, we're using `input_list[::-1]`, which means:

* `start` is omitted, so the slice starts at the beginning of the list.
* `stop` is omitted, so the slice stops at the end of the list.
* `step` is `-1`, which means the slice
```


### Q4_1

```
**Reversing a List in Python**
================================

Here is a Python function that uses slicing to reverse a list:

```python
def reverse_list(input_list):
    """
    Reverses the order of elements in a list.

    Args:
        input_list (list): The list to be reversed.

    Returns:
        list: The reversed list.
    """
    return input_list[::-1]
```

**Example Use Case**
--------------------

```python
# Create a sample list
my_list = [1, 2, 3, 4, 5]

# Reverse the list
reversed_list = reverse_list(my_list)

print(reversed_list)  # Output: [5, 4, 3, 2, 1]
```

This function works by using Python's slice notation `[::-1]`, which tells Python to start at the end of the list and move backwards to the beginning, stepping backwards by 1 element each time.

Alternatively, you can use the `reversed()` function in combination with the `list()` function to achieve the
```


### Q4_HQQ

```
## Reversing a List in Python

You can use slicing to reverse a list in Python. Here's a function that accomplishes this:

```python
def reverse_list(input_list):
    """
    Reverses the order of elements in a given list.

    Args:
        input_list (list): The list to be reversed.

    Returns:
        list: The reversed list.
    """
    return input_list[::-1]
```

### Example Usage:

```python
# Create a sample list
my_list = [1, 2, 3, 4, 5]

# Reverse the list
reversed_list = reverse_list(my_list)

# Print the original and reversed lists
print("Original List:", my_list)
print("Reversed List:", reversed_list)
```

When you run this code, it will output:

```
Original List: [1, 2, 3, 4, 5]
Reversed List: [5, 4, 3, 2, 1]
```

Alternatively, you can use the `re
```


### Q4_HQQ + optimizer

```
**Reversing a List in Python**
================================

You can use slicing to reverse a list in Python. The following function takes a list as input and returns the reversed list.

```python
def reverse_list(input_list):
    """
    Reverses a given list.

    Args:
        input_list (list): The list to be reversed.

    Returns:
        list: The reversed list.
    """
    return input_list[::-1]
```

**Example Use Case**
--------------------

```python
# Create a list
my_list = [1, 2, 3, 4, 5]

# Reverse the list
reversed_list = reverse_list(my_list)

# Print the reversed list
print(reversed_list)  # Output: [5, 4, 3, 2, 1]
```

This function works by using Python's slice notation `input_list[start:stop:step]`. The `start` and `stop` indices are excluded from the slice, and the `step` parameter determines the direction of the slice. A step of
```


## Prompt 3

> Explain the difference between TCP and UDP in three sentences.


### F16

```
TCP (Transmission Control Protocol) is a connection-oriented protocol that guarantees reliable data transfer by establishing a dedicated connection between devices, ensuring data is received in the correct order and retransmitted if lost or corrupted. UDP (User Datagram Protocol), on the other hand, is a connectionless protocol that does not guarantee delivery or order of data, instead relying on packet loss and retransmission to handle errors. This fundamental difference makes TCP more suitable for applications requiring data reliability, such as file transfers and email,
```


### Q4_0

```
TCP (Transmission Control Protocol) is a connection-oriented protocol that guarantees the delivery of data packets in the correct order, with error-checking and retransmission if necessary. UDP (User Datagram Protocol), on the other hand, is a connectionless protocol that prioritizes speed over reliability, allowing packets to be sent independently and without guarantee of delivery or order. This fundamental difference affects the use cases for TCP (e.g., file transfers, email) and UDP (e.g., video streaming, online gaming
```


### Q4_1

```
TCP (Transmission Control Protocol) is a connection-oriented protocol that guarantees reliable data transfer between devices, ensuring that data packets are delivered in the correct order and retransmitted if lost or corrupted. In contrast, UDP (User Datagram Protocol) is a connectionless protocol that does not guarantee delivery or order of packets, making it suitable for applications where speed and efficiency are more important than reliability. Additionally, UDP typically uses a "best effort" delivery approach, while TCP uses a "guaranteed delivery"
```


### Q4_HQQ

```
Here are three sentences explaining the difference between TCP and UDP:

TCP (Transmission Control Protocol) is a connection-oriented protocol that ensures reliable, error-checked data transfer between devices by establishing a dedicated connection before data is sent and verifying the integrity of the data upon receipt. In contrast, UDP (User Datagram Protocol) is a connectionless protocol that allows for faster data transfer but does not guarantee delivery or order of packets, making it suitable for applications where speed is more important than reliability. This fundamental difference in approach
```


### Q4_HQQ + optimizer

```
Here is a three-sentence explanation of the difference between TCP and UDP:

TCP (Transmission Control Protocol) is a connection-oriented protocol that establishes a stable, reliable connection between devices before transmitting data, ensuring that data is delivered in the correct order and reassembled correctly at the receiving end. In contrast, UDP (User Datagram Protocol) is a connectionless protocol that sends data without establishing a connection first, resulting in faster transmission but with no guarantee of reliable delivery or order of packets. This makes TCP
```


## Prompt 4

> A farmer has 17 sheep. All but 9 run away. How many are left? Think step by step.


### F16

```
To find out how many sheep are left, we need to subtract the number of sheep that ran away from the total number of sheep.

The farmer has 17 sheep initially.
All but 9 run away, which means 17 - 9 = 8 sheep run away.

So, we need to subtract the 8 sheep that ran away from the total number of sheep:
17 - 8 = 9

Therefore, there are 9 sheep left.
```


### Q4_0

```
To solve this problem, let's break it down step by step.

Initially, the farmer has 17 sheep.

All but 9 run away, which means 9 sheep stay behind.

So, the number of sheep left is 9.

The answer is 9.
```


### Q4_1

```
To find out how many sheep are left, let's break down the information step by step:

1. The farmer has 17 sheep in total.
2. All but 9 run away. This means that 9 sheep stay behind.

Since "all but 9" implies that the rest (the majority) run away, we can infer that the 9 sheep that stay behind are the ones that didn't run away.

So, to find out how many sheep are left, we simply need
```


### Q4_HQQ

```
To solve this problem, let's break it down step by step.

The farmer has 17 sheep initially.

Then, all but 9 run away. This means that 9 sheep stay behind and 8 sheep run away (since 17 - 9 = 8).

So, the number of sheep left behind is 9.

Therefore, there are 9 sheep left.
```


### Q4_HQQ + optimizer

```
To solve this problem, let's break it down step by step.

The farmer has 17 sheep initially.

We are told that "All but 9 run away". This means that 9 sheep stayed with the farmer.

Since all but 9 run away, that means 9 sheep ran away. To find the number of sheep that ran away, we can subtract 9 from the total number of sheep:

17 (initial sheep) - 9 (sheep that stayed) = 8
```


## Prompt 5

> Summarize the plot of Romeo and Juliet in exactly two sentences.


### F16

```
In the tragic play Romeo and Juliet, two young lovers from feuding families, the Montagues and the Capulets, fall in love and marry in secret, ultimately leading to a series of events that culminate in their deaths. The conflict between their families and the hatred between Romeo and Juliet's families' loyalists ultimately leads to the tragic demise of the two lovers, bringing peace to their families through their sacrifice.
```


### Q4_0

```
Romeo and Juliet, a tragic love story by William Shakespeare, tells the tale of two young lovers from feuding families in Verona, who fall in love at a ball and ultimately decide to marry in secret, defying their families' hatred for each other. The tragic ending unfolds when Romeo mistakenly believes Juliet has died, leading him to poison himself, and Juliet awakens to find Romeo dead, ultimately leading to her own death as well.
```


### Q4_1

```
In the tragic love story of Romeo and Juliet, two young lovers from feuding families in Verona, Italy, fall in love and ultimately decide to marry in secret, but their happiness is short-lived as their families' hatred for each other leads to a series of tragic events. The star-crossed lovers' fate is sealed when Romeo kills Juliet's cousin in a duel, leading to his banishment, and ultimately, the tragic deaths of both Romeo and Juliet.
```


### Q4_HQQ

```
In Romeo and Juliet, two young lovers from feuding families in Verona, Italy, fall in love and decide to marry in secret, but their happiness is short-lived as their families' hatred for each other leads to tragic consequences. Ultimately, the two lovers' desire for each other proves to be their downfall, and they meet a tragic end, bringing peace between their families.
```


### Q4_HQQ + optimizer

```
In the tragic love story of Romeo and Juliet, two young lovers from feuding families in Verona, Italy fall in love and get married in secret, but their families' hatred for each other ultimately leads to their tragic demise. The star-crossed lovers' fate is sealed when Romeo kills Juliet's cousin in a duel, leading to his banishment and ultimately, the death of both lovers in a tomb.
```


## Verdict

The generated Python was executed, not eyeballed. Every type produced a function that returns
`[5,4,3,2,1]` for `[1,2,3,4,5]`:

| Type | Function name | Executed result |
|---|---|---|
| F16 | `reverse_list_reverse_method` | reverses correctly |
| Q4_0 | `reverse_list` | reverses correctly |
| Q4_1 | `reverse_list` | reverses correctly |
| Q4_HQQ | `reverse_list` | reverses correctly |
| Q4_HQQ + optimizer | `reverse_list` | reverses correctly |

Prompt 4 is a trap: "all but 9 run away" means 9 remain, and subtracting is the wrong move.
Only Q4_0 answered 9 outright. Q4_1 and Q4_HQQ set up the right reading and then drifted into the
subtraction; F16 went straight for the subtraction. This tracks nothing about quantization - a 3B
model is simply unreliable on this question, and the run to run spread across types is wider than
any quantization effect.

**No score column is given.** Across 25 generations there is no difference between F16, Q4_0, Q4_1,
Q4_HQQ and Q4_HQQ + optimizer that survives reading them side by side: all five are coherent, all
five produce working code, and all five wobble on the same prompt. Perplexity separates them
(10.571 to 11.475, see the report); five prompts at 100 tokens do not, and putting a 1-5 number in
the table would be inventing a signal this experiment cannot see.
